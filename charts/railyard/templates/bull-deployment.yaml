{{- if .Values.bull.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "railyard.fullname" . }}-bull
  labels:
    {{- include "railyard.labels" . | nindent 4 }}
    app.kubernetes.io/component: bull
spec:
  replicas: {{ .Values.bull.replicas }}
  selector:
    matchLabels:
      {{- include "railyard.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: bull
  template:
    metadata:
      labels:
        {{- include "railyard.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: bull
    spec:
      serviceAccountName: {{ include "railyard.serviceAccountName" . }}
      {{- if .Values.imagePullSecret }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret }}
      {{- end }}
      containers:
        - name: bull
          image: {{ include "railyard.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["ry"]
          args: ["bull", "-c", "/etc/railyard/railyard.yaml"]
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "railyard.fullname" . }}-db
                  key: db-password
            - name: PGVECTOR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "railyard.fullname" . }}-db
                  key: pgvector-password
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "railyard.fullname" . }}-auth
                  key: GH_TOKEN
            {{- include "railyard.authExtraEnv" . | nindent 12 }}
          envFrom:
            {{- include "railyard.authEnvFrom" . | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/railyard
              readOnly: true
            {{- include "railyard.authVolumeMounts" . | nindent 12 }}
          {{- with .Values.bull.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "railyard.fullname" . }}-config
        {{- include "railyard.authVolumes" . | nindent 8 }}
{{- end }}
