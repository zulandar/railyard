apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "railyard.fullname" . }}-overlay-gc
  labels:
    {{- include "railyard.labels" . | nindent 4 }}
    app.kubernetes.io/component: overlay-gc
spec:
  schedule: "0 3 * * *"
  jobTemplate:
    metadata:
      labels:
        {{- include "railyard.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: overlay-gc
    spec:
      template:
        metadata:
          labels:
            {{- include "railyard.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: overlay-gc
        spec:
          serviceAccountName: {{ include "railyard.serviceAccountName" . }}
          {{- if .Values.imagePullSecret }}
          imagePullSecrets:
            - name: {{ .Values.imagePullSecret }}
          {{- end }}
          restartPolicy: OnFailure
          initContainers:
            - name: cocoindex-migrate
              image: {{ include "railyard.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/opt/railyard/cocoindex/.venv/bin/python3"]
              args:
                - /opt/railyard/cocoindex/migrate.py
                - --database-url
                - "postgresql://{{ .Values.pgvector.username }}:$(PGVECTOR_PASSWORD)@{{ include "railyard.pgvectorHost" . }}:{{ .Values.pgvector.port }}/{{ .Values.pgvector.database }}?sslmode={{ .Values.pgvector.sslmode | default "prefer" }}"
              env:
                - name: PGVECTOR_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "railyard.fullname" . }}-db
                      key: pgvector-password
          containers:
            - name: overlay-gc
              image: {{ include "railyard.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["ry"]
              args: ["overlay", "gc", "-c", "/etc/railyard/railyard.yaml"]
              env:
                - name: DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "railyard.fullname" . }}-db
                      key: db-password
                - name: PGVECTOR_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "railyard.fullname" . }}-db
                      key: pgvector-password
              volumeMounts:
                - name: config
                  mountPath: /etc/railyard
                  readOnly: true
          volumes:
            - name: config
              configMap:
                name: {{ include "railyard.fullname" . }}-config
